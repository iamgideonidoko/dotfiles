# TPM scans this file for plugins; it cannot follow source-file inside if-shell.
set -g @plugin "tmux-plugins/tpm"
set -g @plugin "tmux-plugins/tmux-sensible"
set -g @plugin "christoomey/vim-tmux-navigator"
set -g @plugin "tmux-plugins/tmux-yank"
set -g @plugin "tmux-plugins/tmux-resurrect"
set -g @plugin "tmux-plugins/tmux-continuum"
set -g @plugin "sainnhe/tmux-fzf"
set -g @plugin 'omerxx/tmux-floax'
set -g @plugin 'rose-pine/tmux'

# Select config by tmux server socket. AoE sets `tmux.socket_name = "aoe"`.
if-shell -F "#{m:*/aoe,#{socket_path}}" "source-file ~/dotfiles/tmux/aoe.conf" "source-file ~/dotfiles/tmux/main.conf"
